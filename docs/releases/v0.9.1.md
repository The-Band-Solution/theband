# v0.9.1 — o limite passa a valer de verdade, e o CD volta a dizer a verdade

**Decidida por**: proposta do papel `sro.product_owner_role` em 2026-09-23 — **pendente
de confirmação** de `sro.product_owner`
**PR de release**: a abrir (`development → main`) · **Merge/deploy**: pendente
**Aceitação que a sustenta**: [`docs/sprints/033-a-api-publica/aceitacao.md`](../sprints/033-a-api-publica/aceitacao.md),
**com uma reavaliação proposta** — o SC-004, que aquele registro recusou, e que esta
release conserta
**Carga**: **um** commit — `e9cb31e` ([#938](https://github.com/The-Band-Solution/theband/pull/938)).
Nenhuma migração, nenhuma variável de ambiente nova, nenhuma tela alterada

**Medido em 2026-09-23, contra a origem e contra a produção**, e não lembrado:

| O que se mediu | Comando ou consulta | Resultado |
|---|---|---|
| o que separa `main` de `development` | `git log --oneline origin/main..origin/development` | **um** commit: `e9cb31e` |
| migração nova | `git diff --name-only origin/main...origin/development -- priv/repo/migrations/` | **vazio** |
| variável nova | `git diff origin/main...origin/development -- .env.example docker-compose.yml config/runtime.exs \| grep '^+'` | **vazio** |
| tela alterada | mesmo diff, filtrado por `.heex` e `live/` | só `test/.../fluxo_na_tela_test.exs` — **nenhuma tela de produção** |
| a versão no ar | `curl https://theband.5.189.161.85.sslip.io/version` | `0.9.0`, HTTP 200 |
| o `mix.exs` | `grep 'version:' mix.exs` | `0.9.0` — igual ao que a produção serve |

---

## O que esta release entrega

### 1. O limite de taxa passa a valer 120 por minuto de verdade — antes deixava passar 240 na virada

A janela era **fixa**: o contador zerava na virada do minuto, e quem chamasse 120 vezes nos
últimos segundos de um minuto e 120 nos primeiros do seguinte passava **240 em poucos
segundos**, com limite declarado de 120. A limitação estava declarada no #936 quando ele
entrou, e foi para produção assim.

Agora o minuto é contado em **fatias de dez segundos**, e a conta soma as fatias que cobrem
os últimos sessenta. A rajada na virada cai de um minuto inteiro para dez segundos de
imprecisão, e a soma é **mais estrita que a janela declarada, nunca mais frouxa**.

A razão de não ser a janela exata está registrada no PR e importa aqui: guardar o instante de
cada chamada daria a janela perfeita, mas contar exigiria ler, podar e escrever — e isso não
é atômico. Duas requisições simultâneas do mesmo token leriam a mesma lista e **as duas
passariam**, justamente sob a carga que o limite existe para conter. `update_counter` sobre a
fatia é atômico.

**Quem usa a API não vê diferença nenhuma**, exceto quem estava aproveitando a virada.

### 2. Quem opera passa a distinguir cinco causas de recusa no log, e o cliente continua vendo exatamente a mesma resposta

Era o **SC-004**, e o registro de aceitação da 061 o classificou **não conforme** em
2026-09-23: as cinco causas chegavam ao log como `credencial_recusada`, e *"alguém varrendo
tokens que nunca existiram"* e *"credencial que vazou e ainda não foi revogada"* começavam a
mesma investigação. A informação existia e era descartada uma linha antes de chegar ao log.

O log interno agora nomeia: `inexistente`, `malformado`, `revogado`, `expirado`,
`segredo_errado` — mais `sem_cabecalho`, que já existia.

**E a resposta ao cliente continua idêntica nas cinco**, que é o SC-003 e é o que impede
confirmar a quem testa credencial roubada que ela existiu e quando. As duas afirmações são
sustentadas juntas, em lugares diferentes: `api_tokens_test.exs` afirma que os motivos
**internos** diferem; `motivo_da_recusa_test.exs` afirma que as respostas **HTTP** são
idênticas. Nenhum dos dois basta sozinho, e é essa a razão de serem dois.

### 3. O CD volta a dizer a verdade — ele reprovou as duas últimas releases com o deploy funcionando

A conferência final do CD comparava `/version`, que devolve o número puro do `mix.exs`, com o
**nome da tag**, que leva o prefixo `v`. Nunca casava.

Medido nesta avaliação, direto nas execuções do workflow:

| Release | Execução | Resultado | O que a produção respondia |
|---|---|---|---|
| **v0.8.0** | [35609527649](https://github.com/The-Band-Solution/theband/actions/runs/35609527649), 2026-09-21 | **failure** | `404` e `502` nas primeiras tentativas; a partir da 19ª, `0.8.0` — e reprovou mesmo assim, esperando `v0.8.0` |
| **v0.9.0** | [35880777724](https://github.com/The-Band-Solution/theband/actions/runs/35880777724), 2026-09-23 | **failure** | `0.8.0` até a 3ª tentativa; **`0.9.0` a partir da 4ª, às 15:22:25Z** — e reprovou nas trinta, esperando `v0.9.0` |

Duas coisas que essa medição fixa, e que nenhuma delas estava escrita antes:

- **o deploy da v0.9.0 levou 31 segundos** da janela de cinco minutos disponível. A margem é
  larga: o vermelho foi só o formato, e não lentidão de implantação;
- **foram duas releases seguidas reprovadas por engano.** Um CD que reprova quando deu certo
  treina quem lê a ignorá-lo, e a próxima falha real passa despercebida. É por isso que este
  item é o mais urgente dos três, apesar de ser o que menos muda o produto.

---

## Por que 0.9.1, e não 0.10.0 nem 1.0.0

A regra é semver sobre o que a release **carrega**, e a carga é conserto:

| Grau | Cabe? | Por quê |
|---|---|---|
| **MAJOR** | não | nada sai, nenhuma rota some, nenhum campo público muda de significado. A resposta `401` continua byte a byte idêntica nas cinco causas — o SC-003 é preservado **e** testado |
| **MINOR** | não | nenhuma user story nova é entregue. A US5 (*limite de taxa por token*) já foi declarada entregue na v0.9.0; o que muda é a **fidelidade** do limite ao número prometido. Quem usa a API não passa a conseguir nada que não conseguisse ontem |
| **PATCH** | **sim** | conserta sem mudar o prometido: a janela passa a respeitar o limite declarado, o log passa a cumprir a FR-016 que a v0.9.0 descumpria, e o CD passa a comparar o que devia |

**A contra-hipótese, para que ela fique escrita.** Alguém pode argumentar MINOR pelo item 2:
quem **opera** passa a ver cinco motivos onde via um, e isso é capacidade nova para essa
pessoa. Recuso, e a razão é que a FR-016 **já prometia** o motivo real no log interno — a
v0.9.0 foi ao ar sem cumprir, e o registro de aceitação nomeou isso como não conforme.
Cumprir promessa descumprida é retrabalho, e retrabalho é PATCH. Chamar de MINOR faria a
correção de um critério recusado aparecer na série de versões como valor novo, que é
exatamente o que apaga o retrabalho da contagem.

**`0.9.0 → 0.9.1`.** O bump vive na `development`, antes do PR de release, e **não foi feito
por este documento** — é ato do papel, com o PR.

---

## A decisão de escopo: soltar agora com o #938, sem esperar o #939

**Recomendação: (A) — soltar a v0.9.1 só com o #938.** O [#939](https://github.com/The-Band-Solution/theband/pull/939)
fica para a release seguinte.

O #939 está **aberto** (`quality-gates` ainda em execução, conferido em 2026-09-23) e carrega
as três mudanças aprovadas da tela de tokens — *sem expiração* oferecido, motivo na revogação,
painel de uso — mais a migração `20260923120000_motivo_da_revogacao_do_token`, aditiva.

### As seis razões, da mais forte para a mais fraca

**1. O conserto do CD é o instrumento que valida toda release futura, e está não provado.**
Nenhuma execução rodou com ele. Provar um instrumento é exercício de isolar variável: se a
carga for só correção, um vermelho na próxima execução tem **uma** causa candidata. Com
migração e três mudanças de tela juntas, o vermelho fica ambíguo — falhou a comparação, a
migração, a troca de imagem, ou o deploy demorou? Diagnóstico ambíguo em janela de produção
custa mais que uma janela extra.

**2. O que o #938 conserta está em produção errado agora.** Hoje a produção tem janela fixa
— que deixa passar o dobro — e cinco causas de recusa colapsadas numa. Os dois consertos
estão prontos. Adiá-los por um trabalho de **tela** é adiar controle de segurança por
conveniência de empacotamento, e a razão de um não tem nada a ver com a do outro.

**3. O #939 não tem aceitação registrada, e a tela dele tem régua.** A conferência item a
item da estrutura aprovada do `PROMPT.md` contra a tela entregue, com captura ao lado e a
conferência do QA como evidência, **não foi feita**. Juntar os dois cria pressão para aceitar
a tela dentro da janela de deploy — e a regra desta casa é que a tela implementada é
exatamente a tela aprovada. Uma release não é lugar para apressar essa conferência.

**4. O CI do #939 ainda não fechou.** Pré-condição de delivery não atendida **hoje**. Em (B),
o momento da release passa a depender de um evento futuro de duração desconhecida, e o item
que tem urgência própria — o CD vermelho — fica refém dele.

**5. O custo de (A) é uma janela de deploy a mais, e ela é barata.** Sem migração, o delivery
é troca de imagem: nada a restaurar, nada a reverter além de reapontar a tag anterior.

**6. O rollback fica mais simples.** Migração aditiva não perde dado no rollback — isso é
verdade e está medido no #939 —, mas rollback com esquema envolvido ainda exige ordenar
imagem e migração. Nesta release não há o que ordenar.

### O que (A) custa, e fica escrito

| Custo | Tamanho |
|---|---|
| duas conferências de CD, duas notas de release, dois back-merges | real, e pequeno |
| o painel de uso e o motivo da revogação ficam mais um ciclo fora do ar | o painel fecha o item 6 do [plano de correções](../backlog/plano-de-correcoes-da-api.md), e quem opera continua consultando `api_access_reads` à mão |
| *sem expiração* continua indisponível na tela | quem precisa de token sem prazo continua escolhendo um prazo e renovando |

Nenhum dos três é risco de segurança nem defeito em produção. São valor adiado por um ciclo
curto — e o ciclo é curto **porque** esta release é pequena.

### O que a release seguinte carrega, se (A) for confirmada

O #939 vira **v0.10.0**, e MINOR desta vez é o grau certo: três coisas que quem usa **passa a
conseguir fazer** — escolher um token sem prazo, dizer por que revogou, e ver o que a
credencial andou lendo —, mais uma migração aditiva. Ela exige o que esta não exige: o
registro de aceitação da tela contra o protótipo aprovado, e o ensaio de restauração em dia
antes da janela, por regra e não por risco.

---

## A evidência, executada nesta avaliação

Não é a declarada no PR. **Foi executada agora, em árvore limpa da `origin/development`**
(`e9cb31e`), num worktree separado, para que o resultado não carregue nada do #939:

```
mix test test/the_band_web/api/motivo_da_recusa_test.exs \
         test/the_band_web/api/registro_e_limite_test.exs \
         test/the_band/tenants/api_tokens_test.exs
```

**Código de saída: `0` · 43 testes, todos passando.** E a saída do log, que é a evidência
direta do SC-004 — as cinco causas nomeadas, uma a uma, com o `request_id` que liga cada
linha à requisição:

```
[warning] api: credencial recusada · motivo=malformado     request_id=GNgcMjs0D2peU7QAAfjB
[warning] api: credencial recusada · motivo=inexistente    request_id=GNgcMjvdev9eU7QAAfjh
[warning] api: credencial recusada · motivo=segredo_errado request_id=GNgcMjv54NteU7QAAfkB
[warning] api: credencial recusada · motivo=revogado       request_id=GNgcMjwBSDNeU7QAAfkh
[warning] api: credencial recusada · motivo=expirado       request_id=GNgcMjwH6CpeU7QAAflB
```

### A reavaliação que proponho ao papel

| Critério | Fase no registro do sprint 033 | Fase proposta | Evidência |
|---|---|---|---|
| **SC-004** — 100% das recusas com o motivo real recuperável no log interno, razões distintas | **não conforme** | **conforme** | a execução acima: cinco motivos distintos, cada um com `request_id` |
| **SC-003** — inexistente, revogado e expirado com respostas byte a byte idênticas | conforme | **segue conforme** | `motivo_da_recusa_test.exs` passa na mesma execução; o conserto do SC-004 não o quebrou, e é esse o risco que ele tinha |
| **SC-011** — sob limite excedido por um token, outro recebe `200` | conforme | **segue conforme** | `registro_e_limite_test.exs` passa com a janela nova |
| **SC-007** — proveniência e limitações em 100% dos objetos de medida | **não conforme** | **segue não conforme** | nada nesta release toca isso; nasce na [062](../../specs/062-servidor-mcp/), com `KnowledgeBase.measurement/1` |

**O que essa evidência não é.** Os asserts foram escritos por quem implementou; o que executei
foi a suíte deles, e o que li de forma independente foi a saída do log. Isso é mais forte que
a afirmação de quem implementou e mais fraco que uma conferência de terceiro. A distinção fica
aqui em vez de virar "conforme" liso.

**O registro do sprint 033 não foi alterado por este documento.** A mudança de fase do SC-004
é proposta; quem a confirma é `sro.product_owner`, e a confirmação pertence àquele arquivo.

---

## O que esta release NÃO carrega, e alguém pode esperar que carregue

| O que | Estado |
|---|---|
| **as três mudanças aprovadas da tela de tokens** — *sem expiração*, motivo da revogação, painel de uso | no #939, **aberto**; ficam para a v0.10.0 pela decisão de escopo acima |
| **a migração `20260923120000_motivo_da_revogacao_do_token`** | idem — esta release **não tem migração nenhuma** |
| **proveniência nas medidas da API (SC-007)** | continua não conforme; depende da [062](../../specs/062-servidor-mcp/) e de `KnowledgeBase.measurement/1`, que não existe |
| **a versão e as novidades anunciadas na aplicação** | **não existe** superfície: há a rota `/version`, que é endpoint e não página. Ver *O momento do delivery* |
| **o aviso de vencimento por e-mail** | adiado por decisão em 2026-09-23; a plataforma não envia e-mail nenhum — nenhum `Mailer`, nenhuma variável de SMTP |
| **retenção do registro de leitura** | decidida em 2026-09-23 — **indefinida**, registrada em `api.access.thresholds`. Não há poda, e isso é escolha, não descuido |
| **o servidor MCP** | o #935 é plano; nenhum servidor sobe |
| **teto de concorrência** | o limite é por taxa, não por chamadas simultâneas. Medido em produção: sequencialmente **não se alcança** o limite — só com paralelismo |
| **a revisão independente** | **não ocorreu**. Ver as lacunas |

---

## Os três riscos que toda release mede

| Risco | Medida | Resultado |
|---|---|---|
| **migração destrutiva** | `git diff --name-only origin/main...origin/development -- priv/repo/migrations/` | **nenhuma migração**. O ensaio de restauração não é pré-condição desta release |
| **variável obrigatória nova** | diff de `.env.example`, `docker-compose.yml` e `config/runtime.exs` | **nenhuma**. Nada a configurar antes do merge |
| **mudança em tela em uso** | diff filtrado por `.heex` e `live/` | **nenhuma tela de produção**. O único arquivo de tela tocado é um teste, e só no diagnóstico da falha |

É a carga mais leve que uma release deste repositório já levou, e isso não é acaso: é o
desenho da decisão de escopo.

---

## O momento do delivery

Três perguntas antes do merge. **Duas podem ser respondidas; a terceira não.**

| Pergunta | Resposta em 2026-09-23 |
|---|---|
| a aceitação está confirmada pelo papel? | **pendente** — a reavaliação do SC-004 está proposta acima e precisa da confirmação de `sro.product_owner` |
| o CI está verde no PR de release? | **a medir quando o PR existir** |
| **a página anuncia esta versão?** | **não, e a superfície não existe** |

### A terceira pergunta, e a dispensa que eu proponho pela terceira vez

A regra é de 2026-09-09: *a página do The Band sempre carrega a versão da aplicação em
produção e as funcionalidades novas daquela versão*. A v0.7.0 saiu com **dispensa explícita**,
e naquela dispensa está escrito que *"a dispensa não se repete: a v0.8.0 não sai sem isto"*.
Saiu. A v0.9.0 saiu. E proponho que a v0.9.1 saia também.

**O que existe hoje**: a rota `/version`, que devolve `0.9.0` em texto puro. É endpoint, não
página: serve ao CD, e não a quem usa.

**Por que proponho a dispensa aqui, e o que ela custa menos**: esta release **não entrega
funcionalidade nova a quem usa**. Se a superfície existisse, ela diria a versão e diria,
honestamente, que nada novo foi entregue. Nenhum alcance é retirado, nenhuma tela muda debaixo
de ninguém — que era exatamente o caso que a regra nasceu para cobrir, e que a v0.7.0 tinha.

**O que a dispensa não muda**: o item
[`docs/backlog/a-versao-em-producao-na-pagina.md`](../backlog/a-versao-em-producao-na-pagina.md)
continua aberto, e esta é a **quarta** release consecutiva sem ele. A regra tem quatro
releases e zero cumprimentos, e chamar isso de dispensa pontual já não descreve o que está
acontecendo: **é uma regra que o processo não está cumprindo**. Ou o item entra num sprint com
data, ou a regra é revista por quem a estabeleceu. As duas saídas são legítimas; continuar
dispensando a cada release não é nenhuma das duas.

E a v0.10.0, que leva as três mudanças de tela, **não tem essa saída**: ela muda a tela de
tokens debaixo de quem a usa hoje.

---

## As lacunas que embarcam declaradas

**Nenhuma delas é fechada por esta release, e nenhuma deve ser marcada como cumprida por
nenhum merge.**

| Lacuna | Classificação exata | O que falta |
|---|---|---|
| **revisão independente** do desenho da API e da avaliação de segurança | **revisão não ocorreu** | quatro tentativas por agente falharam em 21 e 22 de setembro — duas travaram, duas foram bloqueadas por ferramenta indisponível. Merge não é revisão, e CI verde não é revisão |
| **o #938 foi mergeado sem revisor sequer pedido** | medido: `reviewRequests` e `reviews` **vazios** | o PR nasce com revisor solicitado à equipe `the-band` e ligado ao projeto. Não foi feito, e o resíduo não se recupera: não se pede revisão de PR mergeado |
| **o #939 também está sem revisor pedido** | medido: `reviewRequests` vazio, PR **aberto** | este ainda dá tempo, e é pré-condição para a v0.10.0 — não aceito entregável cujo PR não teve revisão pedida |
| **o token de produção usado nas medições passou por um proxy que intercepta TLS** | **por revogar** | ato da pessoa mantenedora. Enquanto não for revogado, existe uma credencial de produção que já trafegou por intermediário |
| **o 120 do limite é proposta, não medida** | declarado em `api.access.thresholds` | não há uso real para medir contra. Quando houver, o número muda em um lugar só |
| **a causa dos dois testes instáveis** | **desconhecida** | seis execuções da suíte não os reproduziram; o item foi rebaixado para baixa, com diagnóstico acrescentado. Raro não é explicado |

**A do proxy tem ordem de precedência sobre as outras**, e por isso fica dita separada: é a
única que representa exposição ativa hoje, e a única cujo fechamento não depende de ninguém
além de quem mantém.

---

## O que o CD fez

<A preencher depois do merge: digest da imagem, tag, webhook, e — a pergunta desta
release — **se a conferência final passou a comparar o que devia**. É a primeira execução com
o conserto, e é o que esta release existe para provar.>

## O que se observou depois

<A preencher: primeiras horas, se algum cliente bateu no limite com a janela nova, se algum
motivo de recusa apareceu no log e qual, e o que virou issue.>
